version: "3.8"

services:
  gateway:
    build:
      context: .
      dockerfile: app/Dockerfile
    container_name: openclaw-gateway
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./queue:/app/queue
